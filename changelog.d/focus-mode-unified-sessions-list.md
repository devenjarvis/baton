### Changed

Focus Mode now shows a single **SESSIONS** list instead of separate ACTIVE and ATTENTION sections. Every in-progress session appears exactly once with an inline status badge: `✗ error` (red), `⏸ N waiting — <reason>` (orange), `✓ finished — awaiting prompt` (green), or `N active, M idle`. The separate ATTENTION section and its cursor state have been removed.
