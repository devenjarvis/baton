### Fixed

- Pipeline widget PLANNING count now includes sessions in `LifecycleDrafting`, matching the PLANNING section list. Previously a session whose plan was still being drafted appeared in the section but read as `0` in the widget header.
