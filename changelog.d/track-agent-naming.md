### Changed

- Agents are now named **Track 1**, **Track 2**, etc. instead of random adjective-noun placeholders (`brave-falcon`). Names are stable for the agent's lifetime.
- After the first user prompt, the session separator in the sidebar updates to the Haiku-derived task name (e.g. `add-dark-mode`). Previously this update was applied to the agent row instead of the session.
